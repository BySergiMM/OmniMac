#!/bin/sh
# Cierra OmniMac si está abierta, para poder reemplazar el .app sin problemas.
if pgrep -xq OmniMac; then
  i=0
  while pgrep -xq OmniMac && [ $i -lt 12 ]; do
    pkill -x OmniMac 2>/dev/null || true
    sleep 0.25; i=$((i+1))
  done
fi
exit 0
